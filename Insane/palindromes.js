/**
 * Given string with n length, determine if the string is an array, or if the string COULD
 * be a palindrome if we rearrange the letters by any way
 * 
 * For example: 
 *  Oxxo should return true, because is an array
 *  anacilamamlci should return true, because if we rearrange the letters, we can get
 *  acilamnmalica, which is a palindrome.
 *  ecmfcfqojwmznsds should return false, because there's no possible rearrange that 
 *  form a possible palindrome
 *  tabancpicomlemebotipalinza should return true, because if we rearrange the letters, we get
 *  olciptabemanizinamebatpiclo, which is a palindrome. 
 * 
 *  The algorithm should work for ANY string length.
 */

const isPalindrome = (string) =>{

}